name: Push Container to Heroku

on:
  push:
    branches:
      - heroku

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Build, Push and Deploy to Heroku
        id: heroku
        uses: jctaveras/heroku-deploy@v1.0.0
        with:
          email: ${{ secrets.xm14@rice.edu }}
          api_key: ${{ secrets.68af93db-e343-4aaf-a6fc-a84e11bf0353 }}
          app_name: ${{ secrets.deepface-app1 }}
          dockerfile_path: C:\Users\XueyanMu\PycharmProjects\deepface-app1\heroku\Dockerfile
